"""Reads and parses incoming HTTP request bodies for API endpoints."""


class RequestReader:
    def read(self, body: bytes) -> dict:
        """Deserialize an HTTP request body into a validated payload dict."""
        return {}
