"""Reads a continuous stream of data records for pipeline ingestion."""


class StreamReader:
    def read(self) -> list:
        """Consume the next set of records from the live data stream."""
        return []
