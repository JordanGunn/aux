"""Reads and validates authentication tokens from request headers."""


class TokenReader:
    def read(self, header: str) -> dict:
        """Extract and decode an auth token from the Authorization header."""
        return {}
