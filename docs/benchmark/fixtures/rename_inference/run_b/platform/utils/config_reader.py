"""Reads application configuration from YAML files on disk."""


class ConfigReader:
    def read(self, path: str) -> dict:
        """Load and parse a YAML configuration file from the given path."""
        return {}
