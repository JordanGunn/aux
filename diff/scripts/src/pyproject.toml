[project]
name = "diff-skill"
version = "0.1.0"
description = "Deterministic git diff inspection skill"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
